<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - Tusenfryd</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#3b82f6',
                        secondary: '#64748b'
                    }
                }
            }
        }
    </script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">
    <!-- Navigation -->
    <nav class="bg-blue-600 shadow-sm">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <div class="flex items-center">
                    <a href="/" class="flex items-center space-x-2 text-white font-bold text-xl hover:text-blue-100 transition-colors">
                        <i class="bi bi-star-fill text-yellow-400"></i>
                        <span>Tusenfryd</span>
                    </a>
                </div>
                <div class="hidden md:flex items-center space-x-6">
                    <a href="/" class="text-white hover:text-blue-100 transition-colors">Home</a>
                    <a href="/attractions" class="text-white hover:text-blue-100 transition-colors">Attractions</a>
                    <a href="/check-reservation" class="text-white hover:text-blue-100 transition-colors">Check Reservation</a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="flex-1 flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
        <div class="max-w-md w-full text-center">
            <div class="error-container">
                <% if (error.status === 404) { %>
                    <i class="fas fa-map-signs text-6xl text-gray-400 mb-6"></i>
                    <h1 class="text-6xl font-bold text-gray-900 mb-4">404</h1>
                    <h2 class="text-2xl font-semibold text-gray-700 mb-4">Page Not Found</h2>
                    <p class="text-lg text-gray-600 mb-8">The page you're looking for seems to have wandered off like a lost visitor in our park!</p>
                <% } else if (error.status === 500) { %>
                    <i class="fas fa-tools text-6xl text-gray-400 mb-6"></i>
                    <h1 class="text-6xl font-bold text-gray-900 mb-4">500</h1>
                    <h2 class="text-2xl font-semibold text-gray-700 mb-4">Server Error</h2>
                    <p class="text-lg text-gray-600 mb-8">Our attractions are experiencing technical difficulties. Please try again later!</p>
                <% } else if (error.status === 403) { %>
                    <i class="fas fa-ban text-6xl text-gray-400 mb-6"></i>
                    <h1 class="text-6xl font-bold text-gray-900 mb-4">403</h1>
                    <h2 class="text-2xl font-semibold text-gray-700 mb-4">Access Denied</h2>
                    <p class="text-lg text-gray-600 mb-8">You don't have permission to access this area of the park.</p>
                <% } else { %>
                    <i class="fas fa-exclamation-triangle text-6xl text-yellow-500 mb-6"></i>
                    <h1 class="text-6xl font-bold text-gray-900 mb-4"><%= error.status || 'Error' %></h1>
                    <h2 class="text-2xl font-semibold text-gray-700 mb-4">Something went wrong</h2>
                    <p class="text-lg text-gray-600 mb-8">We encountered an unexpected issue. Please try again later.</p>
                <% } %>

                <% if (error.message && process.env.NODE_ENV === 'development') { %>
                    <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4 mb-6">
                        <div class="text-yellow-800">
                            <strong>Error Details:</strong> <%= error.message %>
                        </div>
                    </div>
                <% } %>

                <div class="space-y-3">
                    <a href="/" class="w-full inline-flex items-center justify-center px-6 py-3 bg-blue-600 text-white font-medium rounded-lg hover:bg-blue-700 transition-colors">
                        <i class="fas fa-home mr-2"></i>
                        Back to Park
                    </a>
                    <button onclick="history.back()" class="w-full inline-flex items-center justify-center px-6 py-3 border border-gray-300 text-gray-700 bg-white hover:bg-gray-50 font-medium rounded-lg transition-colors">
                        <i class="fas fa-arrow-left mr-2"></i>
                        Go Back
                    </button>
                </div>

                <div class="mt-6">
                    <p class="text-gray-500">
                        <i class="fas fa-question-circle mr-2"></i>
                        Need help? <a href="mailto:support@tusenfryd.no" class="text-blue-600 hover:text-blue-700 transition-colors">Contact Support</a>
                    </p>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-900 text-white py-8">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex flex-col md:flex-row md:items-center md:justify-between">
                <div class="mb-4 md:mb-0">
                    <h5 class="text-lg font-semibold mb-2">Tusenfryd</h5>
                    <p class="text-gray-400">Norway's most exciting amusement park</p>
                </div>
                <div class="text-gray-400">
                    <p>&copy; 2024 Tusenfryd. All rights reserved.</p>
                </div>
            </div>
        </div>
    </footer>
</body>
</html>
